const initialState = {
  loading: true,
  posts: []
};

  export default function productPageSliceReducer(state = initialState, action) {
    switch (action.type) {
      default: {
        return state;
      }
    }
  }